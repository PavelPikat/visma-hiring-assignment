parameters:
  environmentName: ''
  allowDestructivePlan: ''

steps:
  - template: terraform.yml
    parameters:
      subscription: 'Pay-As-You-Go'
      terraformVersion: '1.2.5'
      workingDirectory: '$(Build.SourcesDirectory)\sre\terraform'
      allowDestructivePlan: '${{ parameters.allowDestructivePlan }}'
      backendKey: '${{ parameters.environmentName }}'
